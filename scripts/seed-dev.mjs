#!/usr/bin/env node
/**
 * Seeds a local development environment with sample data.
 * Currently a placeholder — populated in M2 once the storage layer exists.
 */

import process from 'node:process';

console.log('seed-dev: nothing to seed yet (storage layer arrives in M2)');
process.exit(0);
